spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always

  data:
    redis:
      host: localhost
      port: 6370  # 테스트용 포트 (임베디드 Redis)
      
  cache:
    type: redis