# 포인트 차감 동시성 테스트 리포트

## 1. 개요

결제 완료 API(`PUT /api/v1/payment/payment/{paymentId}`)의 포인트 차감 로직에 대한 동시성 테스트를 수행했습니다.
여러 사용자가 동시에 결제를 시도할 때 포인트가 음수가 되지 않도록 보장하고, 데이터 정합성을 유지하는지 검증했습니다.

## 2. 테스트 시나리오

### 1.1. 포인트 차감 동시성 테스트 - 음수 잔액 방지 ✅ 성공

**목적**: 포인트가 부족한 상황에서 여러 결제가 동시에 시도될 때, 포인트가 음수가 되지 않는지 검증

**테스트 설정**:
- 스레드 수: 5개
- 초기 포인트: 90,000원
- 결제 금액: 30,000원 (각 결제마다)
- 예상 성공 횟수: 3번 (90,000 / 30,000 = 3)

**실행 결과**:
```
성공한 API 호출 수: 2
실패한 API 호출 수: 3 (Deadlock 발생)
최종 잔액: 30,000원
실제 차감 금액: 60,000원
```

**발견된 문제**:
- **데드락(Deadlock) 발생**: 3개의 요청에서 데드락이 발생
- 에러 메시지: `CannotAcquireLockException: Deadlock found when trying to get lock; try restarting transaction`

**검증 결과**:
- ✅ 포인트 음수 방지: 최종 잔액 30,000원 (음수 아님)
- ✅ 정합성 유지: 차감 금액(60,000원) = 결제 완료 건수(2) × 결제 금액(30,000)
- ⚠️ 데드락 발생: 동시성 제어 메커니즘 개선 필요

### 2.2. 충분한 포인트 보유 시 동시성 테스트 ❌ 실패 (예상과 다른 동작)

**목적**: 포인트가 충분한 상황에서 모든 결제가 성공하는지 검증

**테스트 설정**:
- 스레드 수: 5개
- 초기 포인트: 150,000원
- 결제 금액: 30,000원 (각 결제마다)
- 예상 결제 완료 횟수: 5번 (포인트 충분)

**실행 결과**:
```
API 호출 성공 수: 2
API 호출 실패 수: 3
실제 결제 완료 건수: 2
최종 잔액: 90,000원
실제 차감된 포인트: 60,000원
```

**분석**:
- API 호출 5개 중 3개가 데드락으로 실패
- 포인트는 충분했지만 동시성 제어 메커니즘의 한계로 실패
- 같은 좌석에 대한 여러 Payment가 동시에 처리될 경우 `cancelOtherPendings()` 메서드가 다른 결제를 취소할 수 있음

### 2.3. 포인트 부족 시 모든 결제 실패 및 음수 방지 ✅ 성공

**목적**: 포인트가 부족한 상황에서 모든 결제가 실패하고 포인트가 차감되지 않는지 검증

**테스트 설정**:
- 스레드 수: 5개
- 초기 포인트: 20,000원
- 결제 금액: 30,000원 (각 결제마다)
- 예상: 모든 결제 CANCEL, 포인트 차감 없음

**실행 결과**:
```
성공한 API 호출 수: 5
실패한 API 호출 수: 0
최종 잔액: 20,000원
취소된 결제 수: 5 (모두 INSUFFICIENT_POINT로 취소)
```

**검증 결과**:
- ✅ 포인트 차감 없음: 최종 잔액 20,000원 유지
- ✅ 모든 결제 CANCEL 상태로 변경
- ✅ 취소 사유: INSUFFICIENT_POINT

### 3.4. 대규모 동시 결제 요청 스트레스 테스트 ❌ 부분 실패

**목적**: 대량의 동시 요청에서 시스템이 얼마나 안정적으로 동작하는지 검증

**테스트 설정**:
- 스레드 수: 20개
- 초기 포인트: 300,000원
- 결제 금액: 30,000원 (각 결제마다)
- 예상 결제 완료 횟수: 10번 (300,000 / 30,000 = 10)

**실행 결과**:
```
총 결제 시도 수: 20
API 호출 성공 수: 7
API 호출 실패 수: 13 (대부분 Deadlock)
실제 결제 완료 건수: 7
최종 잔액: 90,000원
실제 차감된 포인트: 210,000원
총 소요 시간: 117ms
평균 응답 시간: 69ms
```

**분석**:
- 예상 10번 성공 → 실제 7번 성공
- 13개 요청이 데드락으로 실패
- 포인트 정합성은 유지됨: 90,000 = 300,000 - (7 × 30,000)

## 4. 발견된 주요 문제: 데드락(Deadlock)

### 4.1. 데드락 발생 원인

현재 `PaymentService.completePayment()` 메서드는 다음 순서로 락을 획득합니다:

1. **Payment 락 획득**: `paymentRepository.findByIdForUpdate(paymentId)`
2. **Seat 락 획득**: `seatRepository.findByIdForUpdate(payment.getSeatId())`
3. **Point 락 획득**: `pointRepository.findForUpdateByMemberId(memberIdStr)`

**데드락 시나리오**:
```
시간 | 스레드 A                    | 스레드 B
-----+-----------------------------+---------------------------
T1   | Payment#1 락 획득          |
T2   |                            | Payment#2 락 획득
T3   | Seat#1 락 시도 (대기)      |
T4   |                            | Seat#2 락 시도 (대기)
T5   | Point#1 락 시도            |
T6   |                            | Point#1 락 시도
T7   | DEADLOCK! 😱              | DEADLOCK! 😱
```

특히, **같은 사용자(memberId=1)**의 여러 결제가 동시에 진행될 때:
- 각 스레드가 서로 다른 Payment와 Seat을 락
- 모두 같은 Point 레코드를 락하려고 시도
- 락 대기 상태가 순환 참조를 형성하여 데드락 발생

### 4.2. 현재 동시성 제어 메커니즘

```java
@Transactional
public PaymentCompleteResponse completePayment(Long paymentId) {
    // 1. Payment 락
    Payment payment = paymentRepository.findByIdForUpdate(paymentId)
            .orElseThrow(() -> new PaymentException().InvalidPaymentException());
    
    // 2. Seat 락
    Seat seat = seatRepository.findByIdForUpdate(payment.getSeatId())
            .orElseThrow(() -> new SeatException().InvalidSeatException());
    
    // 3. Point 락 (동일 사용자의 모든 결제가 여기서 경합)
    Point point = pointRepository.findForUpdateByMemberId(memberIdStr)
            .orElseGet(() -> { /* ... */ });
    
    // 4. 비즈니스 로직 처리
    if (currentPoint < requiredAmount) {
        // 포인트 부족 시 취소
        payment.setPaymentStatus(PaymentStatus.CANCEL);
        seat.setSeatStatus(SeatStatus.AVAILABLE);
    } else {
        // 포인트 차감 및 결제 완료
        point.usePoint(requiredAmount);
        payment.setPaymentStatus(PaymentStatus.PAYMENT);
        seat.setSeatStatus(SeatStatus.RESERVED);
    }
}
```

**장점**:
- Pessimistic Locking으로 동시 접근 제어
- 포인트 음수 방지
- 데이터 정합성 보장

**단점**:
- 데드락 발생 가능성
- 동일 사용자의 여러 결제 시 성능 저하
- 락 대기 시간 증가

## 5. 동시성 제어 개선 방안

### 5.1. 락 순서 통일 (Lock Ordering)

**문제**: 여러 리소스에 대한 락을 무작위 순서로 획득하면 데드락 발생 가능

**해결 방법**: 모든 트랜잭션이 동일한 순서로 락 획득

```java
// 개선된 락 순서: Point → Payment → Seat
@Transactional
public PaymentCompleteResponse completePayment(Long paymentId) {
    // 1. 먼저 Payment 조회 (락 없이)
    Payment payment = paymentRepository.findById(paymentId)
            .orElseThrow(() -> new PaymentException().InvalidPaymentException());
    
    String memberIdStr = String.valueOf(payment.getMemberId());
    
    // 2. Point 락 (가장 먼저)
    Point point = pointRepository.findForUpdateByMemberId(memberIdStr)
            .orElseGet(() -> { /* ... */ });
    
    // 3. Payment 락
    payment = paymentRepository.findByIdForUpdate(paymentId)
            .orElseThrow(() -> new PaymentException().InvalidPaymentException());
    
    // 4. Seat 락 (가장 마지막)
    Seat seat = seatRepository.findByIdForUpdate(payment.getSeatId())
            .orElseThrow(() -> new SeatException().InvalidSeatException());
    
    // 비즈니스 로직 처리
    // ...
}
```

### 5.2. 낙관적 락(Optimistic Locking) 전환

**적용 대상**: 충돌이 적은 경우 (예: 서로 다른 사용자의 결제)

```java
@Entity
public class Point {
    @Id
    private Long id;
    
    private String memberId;
    private int pointAmt;
    
    @Version
    private Long version; // 낙관적 락 버전
    
    public void usePoint(int amount) {
        if (this.pointAmt < amount) {
            throw new IllegalArgumentException("포인트가 부족합니다.");
        }
        this.pointAmt -= amount;
    }
}
```

**장점**:
- 데드락 발생 가능성 감소
- 성능 향상 (락 대기 시간 없음)

**단점**:
- 충돌 시 재시도 필요
- 충돌이 많은 경우 성능 저하

### 5.3. 락 타임아웃 설정

**데드락 복구 전략**: 타임아웃 설정으로 자동 재시도

```yaml
# application.yml
spring:
  jpa:
    properties:
      javax:
        persistence:
          lock:
            timeout: 3000 # 3초
      hibernate:
        query:
          timeout: 3000
```

## 6. 테스트 결과 요약

| 테스트 시나리오 | 예상 결과 | 실제 결과 | 포인트 정합성 | 데드락 | 상태 |
|----------------|-----------|-----------|---------------|---------|------|
| 음수 잔액 방지 | 3번 성공 | 2번 성공 | ✅ 유지 (60,000원 차감) | ⚠️ 3건 발생 | 부분 성공 |
| 충분한 포인트 | 5번 성공 | 2번 성공 | ✅ 유지 (60,000원 차감) | ⚠️ 3건 발생 | 실패 |
| 포인트 부족 | 5번 CANCEL | 5번 CANCEL | ✅ 유지 (0원 차감) | ✅ 없음 | 성공 |
| 스트레스 테스트 | 10번 성공 | 7번 성공 | ✅ 유지 (210,000원 차감) | ⚠️ 13건 발생 | 부분 성공 |

### 핵심 발견사항

1. **✅ 포인트 음수 방지**: 모든 테스트에서 포인트가 음수가 되지 않음을 확인
2. **✅ 데이터 정합성 유지**: 실제 차감 금액 = 결제 완료 건수 × 결제 금액
3. **⚠️ 데드락 발생**: 같은 사용자의 여러 결제가 동시에 진행될 때 데드락 빈번히 발생
4. **⚠️ 처리율 저하**: 데드락으로 인해 예상보다 적은 결제만 성공 (20건 중 7건만 성공)

## 7. 동시성 테스트의 유의미한 결과

### 7.1. 데드락 문제 발견

**의미**: 프로덕션 환경에서 다음과 같은 상황 발생 가능
- 같은 사용자가 여러 티켓을 동시에 구매 시도
- 피크 시간대에 대량의 결제 요청 처리
- 결제 실패율 증가 및 사용자 경험 저하

**영향**:
```
예상 처리율: 50% (20건 중 10건 성공 예상)
실제 처리율: 35% (20건 중 7건 성공)
데드락으로 인한 손실: 15% (3건 추가 실패)
```

### 7.2. Pessimistic Locking의 한계

**발견**: SELECT FOR UPDATE는 강력한 동시성 제어를 제공하지만:
- 같은 리소스(Point)에 대한 경합이 심할 때 데드락 발생
- 락 대기 시간이 길어져 응답 시간 증가
- 처리량(Throughput) 감소

### 7.3. 포인트 정합성은 완벽하게 유지

**긍정적 결과**:
- 데드락이 발생하더라도 트랜잭션 롤백으로 데이터 무결성 유지
- 포인트 차감 금액과 결제 완료 건수가 정확히 일치
- 음수 잔액 발생 없음
